import styles from "./Navbar.module.scss";

const index = () => {
  return (
    <nav className={styles.container}>
      <div className={styles.maxWidthContainer}>Navbar</div>
    </nav>
  );
};

export default index;
